<h1><%= link Schoolhub.Courses.get_course!(@course_id).name, to: Routes.course_path(@conn, :show, @course_id) %></h1>
<h2>Edit Grade</h2>

<%= render "edit_form.html", Map.put(assigns, :action, Routes.course_affiliation_grade_path(@conn, :update, @course_id, @affiliation_id, @grade)) %>

<span><%= link "Back", to: Routes.course_affiliation_grade_path(@conn, :show,
	  @course_id, @affiliation_id, @grade) %></span>
