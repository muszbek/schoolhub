<!DOCTYPE html>
<html lang="en">
  <head>
    <%= csrf_meta_tag() %>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Schoolhub</title>
    <link rel="stylesheet" href="<%= Routing.route(:static_path, @conn, ["/css/app.css"]) %>"/>
    <script defer type="text/javascript" src="<%= Routing.route(:static_path, @conn, ["/js/app.js"]) %>"></script>
  </head>
  <body>
    <header>
      <section class="container">
        <nav role="navigation">
          <ul>
	    <li><%= link "Router", to: "/router" %></a></li>
            <li><a href="<%= get_session_url(@conn) %>"><%= get_session_link_text(@conn) %></a></li>
            <%= if function_exported?(:"SchoolhubWeb.Router.Helpers", :live_dashboard_path, 2) do %>
              <li><%= link "LiveDashboard", to: Routing.route(:live_dashboard_path, @conn, [:home]) %></li>
            <% end %>
          </ul>
        </nav>
        <a href="<%= Routing.route(:page_path, @conn, [:index]) %>" class="schoolhub-logo">
          <img src="<%= Routing.route(:static_path, @conn, ["/images/schoolhub.png"]) %>" alt="Schoolhub Logo"/>
        </a>
      </section>
    </header>
    <main role="main" class="container">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <%= @inner_content %>
    </main>
  </body>
</html>
