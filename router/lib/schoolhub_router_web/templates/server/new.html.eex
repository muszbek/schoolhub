<h1>New Server</h1>

(This is where you pay money)</br>

<%= render "form.html", Map.put(assigns, :action, Routes.server_path(@conn, :create)) %>

<span><%= link "Back", to: Routes.page_path(@conn, :index) %></span>
